$(".book-comments").html("<%= j(render('books/book-comments', book: @book)) %>");
$("textarea[name='book_comment[comment]']").val('');
window.bookCommentsData = <%= @daily_book_comments.values.reverse.to_json %>;
window.initializeOrUpdateChart(window.bookCommentsData);

